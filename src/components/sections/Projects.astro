---
import {
  Code2,
  Rocket,
  Target,
  TrendingUp,
  Github,
  ExternalLink,
} from "lucide-astro";
import ProjectCard from "@/components/projects/ProjectCard.astro";
import Link from "@/components/ui/Link.astro";
import {
  getProjectStats,
} from "@/data/projects";
import { getLocalizedProjects } from "@/data/getLocalizedProjects";
import { getTranslation } from "@/features/i18n/domain/getTranslation";
import type { Language } from "@/features/i18n/config/languages";

interface Props {
  lang: Language;
  title?: string;
  subtitle?: string;
  showAll?: boolean;
}

const { lang, ...otherProps } = Astro.props;
const t = getTranslation(lang);

const {
  title = t.projects.TITLE,
  subtitle = t.projects.SUBTITLE,
  showAll = false,
} = otherProps;

const localizedProjects = getLocalizedProjects(lang);
const featuredProjects = localizedProjects.sort((a, b) => a.priority - b.priority).slice(0, 3);
const displayProjects = showAll ? localizedProjects : featuredProjects;
const stats = getProjectStats();
---

<section class="opacity-0 py-20 animate-fade-in-up">
  <div class="max-w-7xl mx-auto">
    <!-- Header -->
    <div class="text-center mb-16">
      <div class="flex items-center justify-center mb-6">
        <div class="p-3 bg-violet-100 dark:bg-violet-900/30 rounded-full">
          <Code2 class="w-8 h-8 text-violet-600 dark:text-violet-400" />
        </div>
      </div>

      <h2
        class="text-3xl sm:text-4xl font-bold text-zinc-900 dark:text-white mb-4"
      >
        {title}
      </h2>

      <p class="text-lg text-violet-600 dark:text-violet-400 font-medium mb-6">
        {subtitle}
      </p>

      <p
        class="text-zinc-600 dark:text-zinc-400 max-w-3xl mx-auto leading-relaxed mb-8"
      >
        {t.projects.INTRO_PARAGRAPH}
      </p>

      <!-- Quick Stats -->
      <div class="grid grid-cols-2 md:grid-cols-3 gap-6 max-w-2xl mx-auto">
        <div class="text-center">
          <div class="text-2xl font-bold text-violet-600 dark:text-violet-400">
            {stats.completed}
          </div>
          <div class="text-sm text-zinc-600 dark:text-zinc-400">
            {t.projects.COMPLETED_LABEL}
          </div>
        </div>

        <div class="text-center">
          <div class="text-2xl font-bold text-blue-600 dark:text-blue-400">
            {stats.inProgress}
          </div>
          <div class="text-sm text-zinc-600 dark:text-zinc-400">
            {t.projects.IN_PROGRESS_LABEL}
          </div>
        </div>

        <div class="text-center">
          <div class="text-2xl font-bold text-green-600 dark:text-green-400">
            {stats.techCount}+
          </div>
          <div class="text-sm text-zinc-600 dark:text-zinc-400">
            {t.projects.TECHNOLOGIES_LABEL}
          </div>
        </div>

        <!-- <div class="text-center">
          <div class="text-2xl font-bold text-orange-600 dark:text-orange-400">
            {stats.completionRate}%
          </div>
          <div class="text-sm text-zinc-600 dark:text-zinc-400">
            Tasa de éxito
          </div>
        </div> -->
      </div>
    </div>

    <!-- What Makes My Projects Stand Out -->
    <div class="mb-16">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div
          class="bg-linear-to-br from-violet-50 to-purple-50 dark:from-violet-900/20 dark:to-purple-900/20 rounded-2xl p-6 border border-violet-200 dark:border-violet-800"
        >
          <div class="flex items-center gap-3 mb-4">
            <div class="p-2 bg-violet-200 dark:bg-violet-800 rounded-lg">
              <Target class="w-5 h-5 text-violet-600 dark:text-violet-400" />
            </div>
            <h3 class="font-semibold text-violet-900 dark:text-violet-100">
              {t.projects.PROBLEM_ORIENTED_TITLE}
            </h3>
          </div>
          <p
            class="text-sm text-violet-700 dark:text-violet-300 leading-relaxed"
          >
            {t.projects.PROBLEM_ORIENTED_DESC}
          </p>
        </div>

        <div
          class="bg-linear-to-br from-blue-50 to-cyan-50 dark:from-blue-900/20 dark:to-cyan-900/20 rounded-2xl p-6 border border-blue-200 dark:border-blue-800"
        >
          <div class="flex items-center gap-3 mb-4">
            <div class="p-2 bg-blue-200 dark:bg-blue-800 rounded-lg">
              <Rocket class="w-5 h-5 text-blue-600 dark:text-blue-400" />
            </div>
            <h3 class="font-semibold text-blue-900 dark:text-blue-100">
              {t.projects.PRODUCTION_READY_TITLE}
            </h3>
          </div>
          <p class="text-sm text-blue-700 dark:text-blue-300 leading-relaxed">
            {t.projects.PRODUCTION_READY_DESC}
          </p>
        </div>

        <div
          class="bg-linear-to-br from-green-50 to-emerald-50 dark:from-green-900/20 dark:to-emerald-900/20 rounded-2xl p-6 border border-green-200 dark:border-green-800"
        >
          <div class="flex items-center gap-3 mb-4">
            <div class="p-2 bg-green-200 dark:bg-green-800 rounded-lg">
              <TrendingUp class="w-5 h-5 text-green-600 dark:text-green-400" />
            </div>
            <h3 class="font-semibold text-green-900 dark:text-green-100">
              {t.projects.MEASURABLE_IMPACT_TITLE}
            </h3>
          </div>
          <p class="text-sm text-green-700 dark:text-green-300 leading-relaxed">
            {t.projects.MEASURABLE_IMPACT_DESC}
          </p>
        </div>
      </div>
    </div>

    <!-- Projects Grid -->
    <div class="space-y-8">
      <!-- Featured Project (if exists) -->
      {
        featuredProjects.length > 0 && (
          <div>
            <h3 class="text-xl font-semibold text-zinc-900 dark:text-white mb-6 flex items-center gap-2">
              <span>🌟</span>
              {t.projects.FEATURED_PROJECT}
            </h3>
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
              <ProjectCard project={featuredProjects[0]} featured={true} lang={lang} />
            </div>
          </div>
        )
      }

      <!-- Other Projects -->
      {
        displayProjects.length > 1 && (
          <div>
            <h3 class="text-xl font-semibold text-zinc-900 dark:text-white mb-6 flex items-center gap-2">
              <span>🚀</span>
              {showAll ? t.projects.ALL_PROJECTS : t.projects.OTHER_PROJECTS}
            </h3>
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
              {displayProjects.slice(1).map((project) => (
                <ProjectCard project={project} lang={lang} />
              ))}
            </div>
          </div>
        )
      }
    </div>

    <!-- Call to Action -->
    <div class="mt-16 text-center">
      <div
        class="bg-linear-to-r from-violet-50 to-zinc-50 dark:from-violet-950/30 dark:to-zinc-900 rounded-2xl p-8 border border-violet-200 dark:border-violet-800"
      >
        <div class="max-w-2xl mx-auto space-y-6">
          <h3 class="text-2xl font-bold text-zinc-900 dark:text-white">
            {t.projects.COLLABORATE_QUESTION}
          </h3>

          <p class="text-zinc-600 dark:text-zinc-300 leading-relaxed">
            {t.projects.COLLABORATE_DESCRIPTION}
          </p>

          <div class="flex flex-wrap justify-center gap-4">
            <Link
              href="https://github.com/FabiSax12"
              variant="hoverEffect"
              class="group"
            >
              <Github class="w-4 h-4 mr-2" />
              {t.projects.VIEW_GITHUB}
              <ExternalLink
                class="w-4 h-4 ml-2 opacity-0 group-hover:opacity-100 transition-opacity"
              />
            </Link>

            <Link href="mailto:fabidev18@gmail.com" variant="solid">
              {t.projects.TALK_PROJECT}
            </Link>
          </div>
        </div>
      </div>
    </div>

    <!-- Tech Philosophy -->
    <!-- <div class="mt-16">
      <div
        class="bg-zinc-50 dark:bg-zinc-800/50 rounded-2xl p-8 border border-zinc-200 dark:border-zinc-700"
      >
        <h3
          class="text-xl font-semibold text-zinc-900 dark:text-white mb-6 text-center"
        >
          💭 Mi Filosofía de Desarrollo
        </h3>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-sm">
          <div class="space-y-4">
            <div>
              <h4 class="font-medium text-zinc-900 dark:text-white mb-2">
                🎯 Código con Propósito
              </h4>
              <p class="text-zinc-600 dark:text-zinc-400">
                Cada línea de código debe resolver un problema específico. No
                escribo código por escribir.
              </p>
            </div>

            <div>
              <h4 class="font-medium text-zinc-900 dark:text-white mb-2">
                🧪 Test-Driven
              </h4>
              <p class="text-zinc-600 dark:text-zinc-400">
                Los tests no son opcionales. Desarrollo con TDD para asegurar
                calidad desde el día uno.
              </p>
            </div>
          </div>

          <div class="space-y-4">
            <div>
              <h4 class="font-medium text-zinc-900 dark:text-white mb-2">
                📚 Documentación Viva
              </h4>
              <p class="text-zinc-600 dark:text-zinc-400">
                La documentación debe evolucionar con el código. README
                completos y APIs autodocumentadas.
              </p>
            </div>

            <div>
              <h4 class="font-medium text-zinc-900 dark:text-white mb-2">
                🔄 Iteración Continua
              </h4>
              <p class="text-zinc-600 dark:text-zinc-400">
                Prefiero entregar valor rápido e iterar basado en feedback real
                de usuarios.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div> -->
  </div>
</section>
